@model IEnumerable<RamirezExamenProgreso1.Models.PlanRecompensas>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NombreRecompensa)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PuntosRecompensa)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NombreRecompensa)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PuntosRecompensa)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.RecompensasId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.RecompensasId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.RecompensasId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
